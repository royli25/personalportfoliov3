/**
 * The Playground desktop — a machine you can launch the products on.
 *
 *   import { Desktop } from "@/desktop";
 *
 * Apps come from `registry.tsx`; a new project is one entry there and nothing
 * else. See desktop.tsx for how the stage is put together.
 */
export { Desktop } from "./desktop";
export { APPS, type DesktopApp } from "./registry";
